export function Header() {
  return (
    <header
      style={{
        background: "gray",
        width: "100%",
        height: 100,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Header
    </header>
  );
}
